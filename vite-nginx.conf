server {
    listen 80;

    location /app/calm-breath {
        alias /usr/share/nginx/html/app/calm-breath;
        index index.html index.htm;
        try_files $uri $uri/ /app/calm-breath/index.html;
    }

    location = / {
        return 301 /app/calm-breath/;
    }

    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|otf)$ {
        root /usr/share/nginx/html;
        expires 1y;
        add_header Cache-Control "public";
    }
}